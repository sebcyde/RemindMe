import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const LocationDetailsModal = (props: Props) => {
	return (
		<View>
			<Text>LocationDetailsModal</Text>
		</View>
	);
};

export default LocationDetailsModal;
